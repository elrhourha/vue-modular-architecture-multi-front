{
  "name": "university-apps",
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --recursive --parallel --filter=!ui-components dev",
    "build": "pnpm --recursive --parallel  build",
    "preview": "vite preview",
    "type-check": "vue-tsc --noEmit"
  }
}
